# Sentence Similarity — BERT Embeddings

Uses a pre-trained SentenceTransformer (BERT-based) model to compute semantic similarity between sentences using cosine similarity, with heatmap visualisation.

**Key concepts:** NLP, semantic similarity, sentence embeddings, cosine similarity, heatmaps

**Tools:** SentenceTransformers, scikit-learn, Matplotlib, Seaborn
